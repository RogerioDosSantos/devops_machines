# ~/.bashrc: executed by bash(1) for non-login shells.

# Note: PS1 and umask are already set in /etc/profile. You should not
# need this unless you want different defaults for root.
# PS1='${debian_chroot:+($debian_chroot)}\h:\w\$ '
# umask 022

# You may uncomment the following lines if you want `ls' to be colorized:
# export LS_OPTIONS='--color=auto'
# eval "`dircolors`"
# alias ls='ls $LS_OPTIONS'
# alias ll='ls $LS_OPTIONS -l'
# alias l='ls $LS_OPTIONS -lA'
#
# Some more alias to avoid making mistakes:
# alias rm='rm -i'
# alias cp='cp -i'
# alias mv='mv -i'

# Schneider Proxy Configuration
http_proxy=http://gateway.zscaler.net:9480
# http_proxy=
export http_proxy
https_proxy=http://gateway.zscaler.net:9480
# https_proxy=
export https_proxy
ftp_proxy=http://gateway.zscaler.net:9480
# ftp_proxy=
export ftp_proxy
socks_proxy=http://gateway.zscaler.net:9480
# socks_proxy=
export socks_proxy
no_proxy=127.0.0.1,192.168.99.100,swtfsiws,dev.wonderware.com
export no_proxy

